package com.example.bookingapplicationas

import android.content.Context
import androidx.appcompat.app.AppCompatActivity
import android.net.*

class verify_network : AppCompatActivity() {

    /*fun verifyAvailableNetwork(activity:AppCompatActivity):Boolean
    {
        var connectivityManager=activity.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
        var networkInfo = connectivityManager.getActiveNetwork
        return  networkInfo != null && networkInfo.equals(true)
    }*/

    /*fun netwok_isAvailable(activity:AppCompatActivity) :Boolean {
        var connectivityManager = activity.getSystemService(Context.CONNECTIVITY_SERVICE) as ConnectivityManager
        var activeNetwork: NetworkInfo? = connectivityManager.getActiveNetworkInfo()
        var isConnected: Boolean = activeNetwork?.isConnectedOrConnecting == true

        return isConnected
    }*/
}